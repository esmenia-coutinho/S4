# NOTAS DE RESUMO

cd 'caminho da pasata
git status
git init
git status
git add .
git status
git commit -m "comentario"
-- git push --set -upstream origin master
-- git push -u origin main
git push -u origin master
git status 